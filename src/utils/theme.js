import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#2AA7FF",
        },
        secondary: {
            main: "#FFFFFF",
        },

        customPrimary: "#2AA7FF",
        customSecondary: "#FFFFFF",
    },
});

export default theme;
